# GD Library

To run WordPress with our themes correctly, you must have the PHP GD library installed and enabled on your server. This is a basic server function required to process images.

If it is not enabled, please contact your hosting provider and ask them to enable the PHP GD library. This is a standard feature on all reputable hosts and should be enabled at no extra cost.
